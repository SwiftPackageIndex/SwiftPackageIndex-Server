{
    "name": "spi-base",
    "build": { "dockerfile": "Dockerfile" },
    "forwardPorts": [8080],
    "customizations": {
      "vscode": {
        "extensions": [
          "swiftlang.swift-vscode"
        ],
        "settings": {
          "lldb.library": "/usr/lib/liblldb.so"
        }
      }
    },
    "runArgs": [
      "--network=spi_test"
    ],
    "containerEnv": {
      "RUNNING_IN_DEVCONTAINER": "1"
    },
    "mounts": [
      {
        "source": "/var/run/docker.sock",
        "target": "/var/run/docker.sock",
        "type": "bind"
      }
    ]
}
